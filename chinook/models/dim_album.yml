version: 2

models:
  - name: dim_album
    description: "Dimension table for music albums"
    columns:
      - name: album_id
        tests: [not_null, unique]
      - name: album_title
        tests: [not_null]
      - name: artist_id
        tests: [not_null]
